import { SetStyleDirective } from './set-style.directive';

describe('SetStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new SetStyleDirective();
    expect(directive).toBeTruthy();
  });
});
